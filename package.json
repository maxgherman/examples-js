{
  "name": "tioc-lazy-resolution-js",
  "version": "0.0.0",
  "private": true,
  "dependencies": {
    "typeioc": "^2.1.8"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "devDependencies": {
    "react-scripts": "latest"
  }
}